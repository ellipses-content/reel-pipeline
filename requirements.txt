# This file lists everything that needs to be installed for the pipeline
# to work. Install all of it at once by running, from this folder:
#
#     pip install -r requirements.txt
#
# What each one is for:

anthropic                   # talks to Claude, writes the video scripts
edge-tts                    # free text-to-speech, makes the voiceover
requests                    # used to download stock clips from Pexels
moviepy==1.0.3               # stitches video, audio, and captions together
python-dotenv                # loads your API keys from the .env file safely
google-auth-oauthlib         # handles YouTube login/authorization
google-api-python-client     # uploads videos to YouTub
openai-whisper
numpy<2.0